enum ProfilePageEditStatus { IDLE_MODE, ACCOUNT_EDIT_MODE, COMPANY_EDIT_MODE, CONTACT_EDIT_MODE }

enum PaymentEditMenu { EDIT, DELETE }

enum ImagePickTypes { CAMERA, GALLERY }

enum ImagePickStatus { FREE, PICKED, CROPPED }

enum PostJobsModulesList {
  Description,
  Language,
  Location,
  Visibility,
  Schedule,
  Review,
}

enum Flavor { DEV, QA, PROD }
enum JOBS_ENUM { SEND_MESSAGE, COPY_LINK, ADD_FAV }

enum CONTRACTS_ENUM { SEND_CONTRACT_MESSAGE, PROPOSE_NEW }

enum CALL_EVENTS {
  ACTION_INCOMING_CALL,
  ACTION_CANCEL_CALL,
  ACTION_REJECT,
  ACTION_ACCEPT,
  ACTION_TOGGLE_MUTE,
  ACTION_END_CALL,
  ACTION_RETURN_CALL,
}

enum USERTYPE {
  CLIENT,
  TRANSLATOR,
}

enum SPACE_TYPE {
  SMALL,
  MEDIUM,
  LARGE,
  EXTRA_LARGE,
}
